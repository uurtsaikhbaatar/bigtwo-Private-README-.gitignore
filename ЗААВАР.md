# Дай Ди — Админ гарын авлага

Токен олгох, реклам удирдах бүх команд. Windows PowerShell дээр туршигдсан.

---

## 0. Бэлтгэл (ганц удаа хийнэ)

1. **PowerShell нээх:** Windows товч → `PowerShell` гэж бич → Enter.
   (Notepad биш! Notepad = файл засах, PowerShell = команд ажиллуулах.)

2. **Script зөвшөөрөх** (аль хэдийн хийсэн ✓):
   ```
   Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned
   ```
   Асуувал `Y` дар.

---

## 1. Цонх нээх бүрд (эхэнд нэг удаа)

Шинэ PowerShell цонх нээх бүрд эхлээд:
```
cd D:\Claude\Claude\Projects\Apps\bigtwo\server
```
Цонхоо хаатал энэ хавтаст л байна. Дараагийн командуудад `cd` дахин хэрэггүй.

---

## ⚠️ ХАМГИЙН ЧУХАЛ ДҮРЭМ

**`&&` хэрэглэхгүй.** Windows PowerShell `&&`-г ойлгодоггүй.
Урт мөрийг бүхэлд нь бүү хуул — эхлээд `cd` (дээрх), дараа нь зөвхөн `npm run ...` хэсгийг бич.

---

## 2. ТОКЕН

**Хэн токен хүссэнийг харах:**
```
npm run tokens
```
Хүсэлт байвал нэр, үлдэгдэл, имэйлээр жагсна.

**Токен олгох** (нэр, дараа нь хэмжээ):
```
npm run tokens -- grant uuree 10000
```
`uuree` → хүссэн нэр, `10000` → хүссэн тоо. Хэмжээ бичихгүй бол 1,000,000.

---

## 3. РЕКЛАМ

**Бүх реклам харах** ([id]-г эндээс хараарай):
```
npm run ads
```

**Текст реклам нэмэх:**
```
npm run ads -- text "Гарчиг" "Доор гарах текст" --link=https://example.com
```

**Зурагтай реклам нэмэх** (.png .jpg .jpeg .gif .webp):
```
npm run ads -- add "C:\Users\зам\зураг.jpg" "Гарчиг" --link=https://example.com
```

**Нэмэлт сонголтууд** (text/add-ийн ард залгана):

| Сонголт | Утга | Жишээ |
|---|---|---|
| `--link=` | Дарахад нээгдэх хаяг | `--link=https://fb.com/page` |
| `--from=` | Эхлэх өдөр | `--from=2026-08-01` |
| `--to=` | Дуусах өдөр | `--to=2026-08-31` |
| `--region=` | Байршлаар шүүх | `--region=Ulaanbaatar` |
| `--weight=` | Эргэлтэд хэд дахин их гарах | `--weight=3` |

**Унтраах / асаах / устгах** (`6` → бодит [id]):
```
npm run ads -- off 6
```
```
npm run ads -- on 6
```
```
npm run ads -- rm 6
```

Реклам нэмэнгүүт тоглогчид лобби болон тоглолтын төгсгөлд харна.

---

## 4. Нэмэлт зөвлөгөө

- **Код өөрчилсний дараа:** "push хийе" гэж надад хэл → амьд сайт шинэчлэгдэнэ.
- **Санг нөөцлөх:** 7 хоног тутам нэг удаа (хүсвэл `npm run backup` хэрэгсэл нэмнэ).
- **`.env` файл:** сангийн нууц үг, API түлхүүртэй. Дэлгэцийн зургийг нийтэд бүү тавь. Password manager-т нэг хувь хадга.
- **Домэйн:** daidi13.com жил бүр сунгагдахыг Cloudflare-с шалга.

---

## Товч лавлах — бүх команд

| Юу хийх | Команд |
|---|---|
| Зөв хавтас (эхэнд нэг удаа) | `cd D:\Claude\Claude\Projects\Apps\bigtwo\server` |
| Токен хүсэлт харах | `npm run tokens` |
| Токен олгох | `npm run tokens -- grant НЭР ХЭМЖЭЭ` |
| Реклам жагсаах | `npm run ads` |
| Текст реклам | `npm run ads -- text "Гарчиг" "Текст" --link=…` |
| Зурагтай реклам | `npm run ads -- add "зам" "Гарчиг" --link=…` |
| Реклам унтраах/асаах | `npm run ads -- off ID` · `npm run ads -- on ID` |
| Реклам устгах | `npm run ads -- rm ID` |
